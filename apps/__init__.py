"""Standalone apps built on the Glimi Core kernel (the ``glimi`` package).

Each subpackage here is a *separate application* on the same kernel — proof that
Glimi Core is a reusable core, not a monolith. ``apps/workspace`` is the work
sibling of the Discord "Community" social-sim: both run the same engine.
"""
