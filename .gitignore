# 커뮤니티 데이터 (DB, 토큰, 로그 — 전부 개인 데이터)
communities/
# 공개 데모 전용 인스턴스 데이터 (demo/demo-en 사본) + 그 platform.db
communities-demo/
data-demo/

# 레거시
legacy/
.env
data/

# Dev
dev/

# Logs (레거시)
logs/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Build
*.egg-info/
dist/
build/
*.whl

# Claude Code
.claude/
.env.dev
.glimi.toml
CLAUDE.local.md

# QA 테스트 결과 (대화 발췌가 있어 개인정보 포함 가능)
tests/e2e/results/

# Eval 런 리포트 (생성물 — 매 실행마다 갱신, 커밋 대상 아님)
eval/reports/

# 백업 (개인 데이터)
backups/

# 로컬 프로젝트 가이드 (개인 메모)
GLIMI_PROJECT_GUIDE.md

# 내부 분석·전략 문서 (경쟁사 리서치, 로드맵 등 — 공개 저장소에 올리지 않음)
analysis/

# 개인 커리어/세션 지시서·공고·지원서 (커리어 전략·개인정보 — 공개 저장소에 올리지 않음)
docs/*지시서*.md
docs/[0-9][0-9]_*

# LoRA 가중치 (각 93MB) — 로컬에만 두고 별도 배포
src/glimi_imagegen/loras/*.safetensors

# Glimi Workspace 앱의 first-run 상태 파일 (개인 name/goal — 로컬에만)
apps/workspace/.workspace_state.json

# AI 이미지 생성 드래프트 (브랜드 에셋 Glimi-*.svg/png 만 트래킹, 나머지는 로컬)
resources/logo/
resources/Gemini_Generated_Image_*
resources/ChatGPT Image *

# 개인 보관용 (소개서/제안서/포폴 랜딩 — git 아님)
proposals/
/index.html
